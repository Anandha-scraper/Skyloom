{
  "name": "skyloom-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "mobile",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=shared && npm run build --workspace=frontend",
    "start": "npm run start --workspace=frontend",
    "install:all": "npm install && npm install --workspace=shared && npm install --workspace=frontend",
    "clean": "npm run clean --workspace=shared && npm run clean --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

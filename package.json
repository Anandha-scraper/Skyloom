{
  "name": "skyloom-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "backend": "npm run dev --workspace=backend",
    "frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=shared && npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "start": "npm run start --workspace=frontend",
    "install:all": "npm install && npm install --workspace=shared && npm install --workspace=frontend && npm install --workspace=backend",
    "clean": "npm run clean --workspace=shared && npm run clean --workspace=frontend && npm run clean --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
